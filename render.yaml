services:
  - type: web
    name: study-thesaurus-proxy
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: MW_THESAURUS_KEY
        sync: false
